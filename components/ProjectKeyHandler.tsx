import { useEffect } from 'react';
import { useProjectStore } from './ProjectProvider';

export const ProjectKeyHandler = ({}) => {
  const {} = useProjectStore();
  useEffect(() => {}, []);
  return null;
};
